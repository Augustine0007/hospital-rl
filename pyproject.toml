[project]
name = "hospital-rl"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv>=0.1.13"
]

[project.scripts]
server = "server.app:main"